# Abu Dhabi Event Configuration

# App Display Name (shown in app stores and on device)
app_name: "Abu Dhabi Marathon"

# Bundle Name (short name, used internally)
bundle_name: "ADNOC Abu Dhabi Marathon"

# App ID (internal identifier)
app_id: 33  # TODO: Update with actual Abu Dhabi app ID

# OneSignal Push Notification ID
onesignal_id: "b86aeedd-38d5-4543-90dd-b965a61d44cf"  # TODO: Update with actual Abu Dhabi OneSignal ID

# Single Event Configuration
single_event_url: "https://eventotracker.com/api/v3/api.cfm/config"
single_event_id: "181"  # TODO: Update with actual Abu Dhabi event ID

# Multi Event Configuration (alternative to single_event)
# multi_event_list_url: "https://eventotracker.com/api/v3/api.cfm/events"
# multi_event_list_id: "0"

# Bundle Identifiers (must be unique per app)
android_bundle_id: "nz.netproject.abudhabi"
ios_bundle_id: "nz.netproject.abudhabi"

# App Icon - Cloud URL (will be downloaded during build)
# Recommended: 1024x1024 PNG
# TODO: Add icon URL when available
icon_url: "https://evento-nz.fra1.cdn.digitaloceanspaces.com/abudhabi/Icon-App-1024x1024@1x.png"

# Splash Image - Cloud URL (will be downloaded during build)
# Recommended: 2048x2048 PNG
# TODO: Add splash URL when available
splash_url: "https://evento-nz.fra1.cdn.digitaloceanspaces.com/abudhabi/splash_image.png"

# Firebase Configuration Files - Cloud URLs (will be downloaded during build)
firebase_android_url: "https://evento-nz.fra1.cdn.digitaloceanspaces.com/abudhabi/google-services.json"
firebase_ios_url: "https://evento-nz.fra1.cdn.digitaloceanspaces.com/abudhabi/GoogleService-Info.plist"

# Android Signing Configuration
# Option 1: Download key.properties from cloud URL
# key_properties_url: "https://your-cdn.com/events/abudhabi/key.properties"

# Option 2: Provide individual signing fields (alternative to key_properties_url)
key_store_password: "Storage12"
key_password: "Storage12"
key_alias: "newmaster"
keystore_file_url: '/Users/toddgiles/Library/Mobile\ Documents/com\~apple\~CloudDocs/private-keys/newmaster-keystore'

# Optional: Search bar color (RGBA)
# searchbar_color:
#   r: 0
#   g: 0
#   b: 0
#   a: 0.5

# Optional: Timer feature
# is_timer: false

# Optional: Version Override (if not specified, uses pubspec.yaml or defaults)
version_name: "7.0.0"    # App version name
version_code: 7         # Build number
